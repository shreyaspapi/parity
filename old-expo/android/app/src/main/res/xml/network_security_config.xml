<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow self-signed certificates for local Unraid servers -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user-installed certificates (for self-signed certs) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- For development/debug builds, also trust user certificates -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>

